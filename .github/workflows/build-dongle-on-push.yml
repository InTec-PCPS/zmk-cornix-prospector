name: Build Dongle Firmware (on push to main)
on:
  push:
    branches:
      - main
    paths:
      - "boards/**"
      - "config/**"
      - "build.dongle.yaml"

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      build_matrix_path: build.dongle.yaml
